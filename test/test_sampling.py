from sampling.sampling import Sampler
from utils import *

def test_labelling_consistency(self):
    c = {"min_freq": 3, "max_freq": 10}
    s = Sampler(to_namedtuple(c))
    assert True, ""
    assert False, "bork"

